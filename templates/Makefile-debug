SRC = exceptions.c main.c native.c ops.c progops.c progtypes.c $(wildcard src/*.c)
OBJ = $(SRC:.c=.o)
CFLAGS = -I. -g

all: main

clean:
	rm -f main $(OBJ)

main: $(OBJ)
	$(CC) $(LDFLAGS) $(OBJ) -o $@

.c.o:
	$(CC) -c $(CFLAGS) $< -o $@
