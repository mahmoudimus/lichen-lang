SRC = exceptions.c main.c $(wildcard native/*.c) ops.c progops.c progtypes.c $(wildcard src/*.c)
OBJ = $(SRC:.c=.o)
CFLAGS = -I. -finput-charset=UTF-8 -g
LDFLAGS = -lm -lgc

all: main

clean:
	rm -f main $(OBJ)

main: $(OBJ)
	$(CC) $(LDFLAGS) $(OBJ) -o $@

.c.o:
	$(CC) -c $(CFLAGS) $< -o $@
